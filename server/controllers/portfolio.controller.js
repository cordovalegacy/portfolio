const Portfolio = require('../models/portfolio.model');
//Here we are pulling in the PortfolioSchema from the model
// const bcrypt = require('bcrypt');
//This would be used for User Auth, hashes the password so the user info is secure
module.exports = {}
//Here we write the logic for our backend to have and run secure functions
//The controller can handle the data sensitive tasks because it is more secure than the front end