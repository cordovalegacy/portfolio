const PortfolioController = require('../controllers/portfolio.controller');
//Here we will declare the variable PortfolioController
//It will import the controller file in for use
module.exports = (app) => {
    //Portfolio routes
    //(app) is being passed to as a way to have express manage the routes
    //This will be done by using app.get, app.post, app.put, app.delete, etc. 
    //This will allow the front end to have true CRUD functionality
}